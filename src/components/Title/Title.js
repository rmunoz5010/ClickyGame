import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>CLICK A CAT (Any of them really)</h1>
        <p>Click on an image to earn points, but don't click the same one twice!</p>
    </div>
);

export default Title;

